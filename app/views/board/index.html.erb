<%- model_class = Board -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:boardid) %></th>
      <th><%= model_class.human_attribute_name(:boardName) %></th>
      <th><%= model_class.human_attribute_name(:boardDesc) %></th>
      <th><%= model_class.human_attribute_name(:boardHostName) %></th>
      <th><%= model_class.human_attribute_name(:boardFavouriteid) %></th>
      <th><%= model_class.human_attribute_name(:boardScore) %></th>
      <th><%= model_class.human_attribute_name(:boardArticleid) %></th>
      <th><%= model_class.human_attribute_name(:boardArticlePostName) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @boards.each do |board| %>
      <tr>
        <td><%= link_to board.id, board_path(board) %></td>
        <td><%= board.boardid %></td>
        <td><%= board.boardName %></td>
        <td><%= board.boardDesc %></td>
        <td><%= board.boardHostName %></td>
        <td><%= board.boardFavouriteid %></td>
        <td><%= board.boardScore %></td>
        <td><%= board.boardArticleid %></td>
        <td><%= board.boardArticlePostName %></td>
        <td><%=l board.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_board_path(board), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      board_path(board),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_board_path,
            :class => 'btn btn-primary' %>
